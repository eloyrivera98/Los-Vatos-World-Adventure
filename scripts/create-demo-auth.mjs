const base=process.env.VITE_NEON_AUTH_URL.replace(/\/$/,'');const users=[{name:'Vato Demo',email:'demo-presentacion@losvatos.app',password:'LosVatosDemo!2026'},{name:'Vato Explorador',email:'demo-friend@losvatos.invalid',password:'DemoFriend!2026'}];for(const user of users){const r=await fetch(base+'/sign-up/email',{method:'POST',headers:{'content-type':'application/json','origin':'http://localhost:5173'},body:JSON.stringify(user)});console.log(user.email,r.status);if(!r.ok&&r.status!==422){console.log((await r.text()).slice(0,300))}}