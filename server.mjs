import 'dotenv/config';import express from 'express';import path from 'node:path';import {fileURLToPath} from 'node:url';import {bootstrap,updateProfile,activateSticker,discoverSticker,updateStickerLocation} from './server/api.mjs';const app=express();app.use(express.json({limit:'3mb'}));app.get('/api/health',(req,res)=>res.json({ok:true}));app.get('/api/bootstrap',bootstrap);app.patch('/api/profile',updateProfile);app.post('/api/stickers/activate',activateSticker);app.post('/api/stickers/discover',discoverSticker);app.patch('/api/stickers/:id/location',updateStickerLocation);const dev=process.argv.includes('--dev');if(dev){const {createServer}=await import('vite');const vite=await createServer({server:{middlewareMode:true},appType:'spa'});app.use(vite.middlewares)}else{const root=path.dirname(fileURLToPath(import.meta.url));app.use(express.static(path.join(root,'dist')));app.use((req,res)=>res.sendFile(path.join(root,'dist','index.html')))}const port=Number(process.env.PORT||5173);app.listen(port,()=>console.log(`Los Vatos en http://localhost:${port}`));